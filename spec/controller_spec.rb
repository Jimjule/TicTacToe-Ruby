require 'controller'

describe Controller do
    before(:each) do
        @controller = Controller.new
    end

    # it 'Can see the board' do
    #     expect { Controller.new }.to output("-----\n|...|\n|...|\n|...|\n-----\n").to_stdout
    # end

    # it 'Can see the board' do
    #     expect { Controller.new }.to output("-----\n|...|\n|...|\n|...|\n-----\n").to_stdout
    # end
end
